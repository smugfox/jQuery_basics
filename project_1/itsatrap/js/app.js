//Hide warning
//Show the warning slowly
// $('.warning').hide().show('slow');

// var myCode = function() {
//   $('.warning').hide().show('slow');
// }

// $(document).ready(myCode);

// $(document).ready(function (){
// 	$('.warning').hide().show('slow');
// });

// $(function(){
// 	$('.warning').hide().show('slow');
// });

// By adding the jQuery link and js file at the 
// End of the page, we don't need a ready function

$('.warning').hide().show('slow');